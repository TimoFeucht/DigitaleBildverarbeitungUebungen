# Projekt Lane Detection

# necessary imports
import cv2
import numpy as np
import matplotlib.pyplot as plt
import glob
import os

# parameters: 
# img: image to apply lane detection on
# mtx_cal: calibration matrix
# dist_cal: distortion coefficients
def find_lines(img, ...):  
       
    return #result_img, left_line, right_line, radius, ...
